m=int(input())
n=int(input())
matrix=[]
maxcol=[]
for i in range(m):
    minrow=[]
    for j in range(n): 
         minrow.append(int(input()))
    matrix.append(minrow)
    maxcol.append(min(minrow))
print(max(maxcol))
